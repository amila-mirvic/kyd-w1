/**
 * World 1 scoring (NEW)
 * This file will contain per-task scoring functions for World 1.
 * For now, it exposes placeholders so you can wire it later.
 */

export function scoreTask1(payload) {
  // payload should include: placements, whyClicks, miniScenarioChoice, etc.
  return { score: 0, meters: { curiosity: 0 } };
}

export function scoreTask2(payload) {
  return { score: 0, meters: { powerMapping: 0 } };
}

export function scoreTask3(payload) {
  return { score: 0, meters: { rights: 0 } };
}

export function scoreTask4(payload) {
  return { score: 0, meters: { realLife: 0 } };
}
